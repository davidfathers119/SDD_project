# 規格確認完成報告
## LED控制系統 - ZC702開發板

**日期**: 2026年3月16日  
**狀態**: ✅ 規格確認完成  
**可以開始代碼生成**: 是

---

## ✅ 需求確認總結

### 用戶提出的關鍵需求

1. **✅ 輸入頻率控制**
   - 必須等待LED調整完才能輸入下一個
   - 輸入過快時回傳: "輸入過快 稍等片刻"

2. **✅ 三種模式支持**
   - 使用2-bit DIP開關控制（GPIO_DIP_SW0/SW1, W6/W7腳位）
   - 硬體資料來自Table 1-25

3. **✅ 動態輸入範圍**
   - PS端根據當前模式調整輸入範圍驗證
   - 模式A擴充到0-255的輸入

4. **✅ 安全性需求**
   - 在PS端完整檢查輸入範圍
   - 只有有效輸入才輸出到PL
   - 無效輸入只回報錯誤信息

5. **✅ UART反饋**
   - 使用詳細模式（含十進制、十六進制、二進制、LED列表）

6. **✅ 錯誤處理**
   - 顯示當前模式
   - 顯示可輸入範圍
   - 每次更換模式時發布提示信息

---

## 📋 最終確認的規格

### 硬體配置

#### LED (8個)
| LED | 腳位 | I/O標準 | 功能 |
|-----|------|---------|------|
| LED[0] | E15 | LVCMOS25 | User LED 0 |
| LED[1] | D15 | LVCMOS25 | User LED 1 |
| LED[2] | W17 | LVCMOS25 | User LED 2 |
| LED[3] | W5 | LVCMOS25 | User LED 3 |
| LED[4] | V7 | LVCMOS25 | User LED 4 |
| LED[5] | W10 | LVCMOS25 | User LED 5 |
| LED[6] | P18 | LVCMOS25 | User LED 6 |
| LED[7] | P17 | LVCMOS25 | User LED 7 |

#### DIP開關 (2個) - **新增**
| DIP | 腳位 | I/O標準 | 功能 |
|-----|------|---------|------|
| DIP[0] | W6 | LVCMOS25 | 模式選擇位元0 |
| DIP[1] | W7 | LVCMOS25 | 模式選擇位元1 |

#### UART
- **連接器**: J17 (USB Type-B)
- **鮑率**: 115200 bps
- **格式**: 8-N-1
- **狀態**: ✅ 已在uart_test專案驗證

---

### 功能規格

#### 模式定義（DIP開關控制）

| DIP[1:0] | 模式名稱 | 輸入範圍 | LED顯示邏輯 |
|----------|----------|----------|------------|
| **00** | 模式A: 二進制顯示 | 0-255 | 直接以二進制顯示 |
| **01** | 模式B: 單燈模式 | 0-8 | 每次只亮一個LED |
| **10** | 模式C: 累進模式 | 0-8 | 從LED0累進點亮 |
| **11** | 無效模式 | - | 顯示錯誤信息 |

#### 輸入處理流程

```
接收輸入 
  ↓
檢查LED更新狀態
  ├─ 進行中 → 回傳"輸入過快，稍等片刻"
  └─ 完成 → 繼續
      ↓
讀取DIP開關狀態
  ↓
確定當前模式
  ↓
驗證輸入範圍（根據模式）
  ├─ 有效 → 
  │    ↓
  │   寫入AXI暫存器
  │    ↓
  │   更新LED
  │    ↓
  │   顯示詳細確認（十進制、十六進制、二進制、LED列表）
  │
  └─ 無效 →
       ↓
      顯示錯誤信息
       ↓
      顯示當前模式
       ↓
      顯示可輸入範圍
```

#### UART輸出格式（詳細模式）

**成功輸入** - 模式A, 輸入23:
```
> 23
輸入值: 23 (0x17)
LED狀態: 00010111 (二進制)
點亮的LED: LED0, LED1, LED2, LED4

> 
```

**無效輸入** - 模式B, 輸入'a':
```
> a
錯誤: 無效輸入 'a' (0x61)
非數字字符

當前模式: 模式B (單燈模式)
可輸入範圍: 0-8
  0 = 全滅
  1-8 = 依序點亮LED0-LED7

> 
```

**輸入過快**:
```
> 5
輸入過快，稍等片刻
> 
```

**模式切換**:
```
===============================================
模式切換: 模式A (二進制顯示模式)
===============================================

輸入範圍: 0-255
輸入方式: 輸入數字後按 Enter 確認
顯示邏輯: 以二進制形式顯示於8個LED
  例: 輸入23 → LED = 00010111

> 
```

---

### 安全性需求

#### PS端驗證（必須實現）

1. **輸入範圍檢查**
   ```c
   // 根據當前模式動態檢查
   switch(current_mode) {
       case MODE_A: valid = (value >= 0 && value <= 255); break;
       case MODE_B: valid = (value >= 0 && value <= 8); break;
       case MODE_C: valid = (value >= 0 && value <= 8); break;
   }
   ```

2. **LED更新狀態檢查**
   ```c
   if (led_updating) {
       printf("輸入過快，稍等片刻\r\n");
       return;
   }
   ```

3. **DIP開關狀態驗證**
   ```c
   dip_value = read_dip_switch();
   if (dip_value == 0b11) {
       printf("無效模式，請調整DIP開關\r\n");
       return;
   }
   ```

4. **只有有效輸入才寫入PL**
   ```c
   if (input_valid) {
       // 寫入AXI暫存器
       Xil_Out32(LED_BASE_ADDR, value);
   } else {
       // 只顯示錯誤，不寫入硬體
       display_error_with_range();
   }
   ```

---

## 📊 文檔更新狀態

| 文檔名稱 | 更新內容 | 狀態 |
|----------|----------|------|
| **硬體規格書** | 新增DIP開關章節 | ✅ 完成 |
| **功能需求規格書** | 更新全部需求 | ✅ 完成 |
| **led_pins.xdc** | 新增DIP開關約束 | ✅ 完成 |
| **規格確認檢查清單** | 記錄確認結果 | ✅ 完成 |

---

## 🎯 代碼生成準備就緒

### PL端（FPGA）需要實現

1. **VHDL模組**: `led_control_system.vhd`
   - 輸入端口:
     - `DIP[1:0]` - 模式選擇（但實際模式處理在PS端）
     - AXI4-Lite Slave接口
   - 輸出端口:
     - `LED[7:0]` - LED控制
   - 功能:
     - 接收AXI寫入命令
     - 驅動8個LED
     - 提供狀態回讀

2. **約束文件**: `led_pins.xdc` ✅ 已完成
   - 8個LED腳位
   - 2個DIP開關腳位

### PS端（ARM）需要實現

1. **C程式**: `main.c`
   - UART初始化與處理
   - GPIO初始化（讀取DIP開關）
   - 模式檢測與切換
   - 輸入緩衝與解析
   - 動態範圍驗證
   - 頻率保護機制
   - AXI寫入操作
   - 詳細的用戶反饋
   - 完整的錯誤處理

---

## ✅ 確認事項

- [x] 所有硬體腳位已確認（來自已驗證專案）
- [x] DIP開關腳位已確認（UG850 Table 1-25）
- [x] 三種模式邏輯已定義清楚
- [x] 動態輸入範圍機制已設計
- [x] 安全驗證流程已確定
- [x] 詳細反饋格式已定義
- [x] 錯誤處理方式已明確
- [x] 模式切換提示已設計
- [x] 輸入頻率保護已考慮
- [x] 約束文件已更新

---

## 🚀 可以開始代碼生成

**確認人**: 用戶  
**確認日期**: 2026年3月16日  
**下一步**: 生成符合規格的VHDL和C代碼

---

**規格確認完成！準備進入代碼生成階段。** ✅
