# 異常情況與邏輯檢查清單
## LED控制系統 - ZC702開發板

**文檔版本**: 1.0  
**日期**: 2026年3月16日  
**目的**: 識別潛在的邊界情況、異常處理和邏輯漏洞

---

## 🚨 關鍵異常情況

### 1. DIP開關相關異常

#### 1.1 運行時模式切換
**情況**: 系統運行中用戶改變DIP開關
```
場景1: 正在輸入數字時切換模式
  當前: 模式A，已輸入 "12"（未按Enter）
  切換: DIP從00改為01（模式B）
  問題: 
    - 輸入緩衝區的"12"如何處理？
    - 12超出模式B範圍(0-8)，是否報錯？
  建議: 
    ✅ 清除輸入緩衝區
    ✅ 顯示模式切換提示
    ✅ 重新開始輸入
```

```
場景2: LED正在更新時切換模式
  當前: LED正在點亮（更新中）
  切換: DIP改變
  問題:
    - LED更新是否立即中斷？
    - 還是等更新完成再切換？
  建議:
    ✅ 等待LED更新完成
    ✅ 再切換模式
    ✅ 切換時重置LED為全滅
```

```
場景3: 快速連續切換模式
  操作: DIP開關 00→01→10→00 (1秒內)
  問題:
    - 每次切換都顯示提示信息嗎？
    - 會不會造成UART輸出混亂？
    - 系統能跟上嗎？
  建議:
    ✅ 加入防抖延遲（100ms）
    ✅ 只響應穩定的模式變化
    ⚠️ 需要定義最小切換間隔
```

#### 1.2 DIP開關讀取錯誤
**情況**: GPIO讀取失敗或返回異常值
```
異常值: DIP讀取到11（無效模式）
  問題: 
    - 系統如何反應？
    - 是否允許任何操作？
  當前規格: 顯示"無效模式，請調整DIP開關"
  
  ⚠️ 需要確認:
    - LED是否應該全滅？
    - 是否禁用所有輸入？
    - 如何從無效模式恢復？
  
  建議:
    ✅ LED全滅
    ✅ 禁用輸入處理
    ✅ 每500ms重新檢查DIP
    ✅ 自動恢復到有效模式
```

#### 1.3 DIP開關抖動
**情況**: 機械開關可能產生電氣抖動
```
抖動場景: 
  00 → 01(5ms) → 00(3ms) → 01(穩定)
  
  問題:
    - 會觸發多次模式切換嗎？
    - 可能造成系統不穩定
  
  建議:
    ✅ 實現軟體防抖
    ✅ 讀取到變化後等待100ms
    ✅ 再次確認狀態
    ✅ 兩次讀取一致才認為有效
```

---

### 2. 輸入處理異常

#### 2.1 模式A特殊情況（多位數輸入）
**情況**: 模式A需要輸入多位數字+Enter

```
場景1: 輸入超大數字
  輸入: "999999<Enter>"
  問題: 999999 >> 255
  當前規格: 應該報錯
  
  ⚠️ 需要確認:
    - 何時檢查範圍？輸入時還是Enter後？
    - 錯誤信息是什麼？
    - 緩衝區長度限制？
  
  建議:
    ✅ 限制輸入緩衝區為4個字符（255+'\0'）
    ✅ 第4個字符後拒絕輸入
    ✅ Enter後檢查範圍
    ✅ 顯示: "錯誤: 輸入值999999超出範圍(0-255)"
```

```
場景2: 輸入負數
  輸入: "-5<Enter>"
  問題: 負數如何處理？
  
  建議:
    ✅ 檢測到'-'立即報錯
    ✅ "錯誤: 不支持負數"
```

```
場景3: 輸入前導0
  輸入: "007<Enter>"
  問題: 應該解析為7還是報錯？
  
  建議:
    ✅ 允許前導0
    ✅ atoi()會自動處理
```

```
場景4: 只輸入Enter
  輸入: "<Enter>"（空字符串）
  問題: 如何處理？
  
  建議:
    ✅ 忽略空輸入
    ✅ 不顯示任何信息
    ✅ 繼續等待
```

```
場景5: 輸入到一半按Enter
  輸入: "2<Enter>"（只有一位）
  問題: 是否有效？
  
  建議:
    ✅ 有效輸入
    ✅ 2在範圍內(0-255)
    ✅ 正常處理
```

```
場景6: 輸入非數字字符
  輸入: "12a<Enter>"
  問題: 如何處理？
  
  建議:
    ✅ 方案1: 檢測到'a'立即報錯，清除緩衝區
    ✅ 方案2: Enter後發現非純數字，報錯
    推薦: 方案1（即時反饋）
```

#### 2.2 特殊字符處理
**情況**: 用戶輸入控制字符

```
Backspace (0x08):
  場景: 輸入"12"，按Backspace，再輸入"3<Enter>"
  期望: 最終輸入為"13"
  
  ⚠️ 問題:
    - 當前規格沒有定義Backspace處理
    - 是否支持編輯？
  
  建議:
    選項1: ✅ 支持Backspace（用戶友好）
    選項2: ❌ 不支持，顯示錯誤
    推薦: 選項1
    
    實現:
    - 刪除緩衝區最後一個字符
    - UART回顯 "\b \b"（刪除效果）
```

```
ESC (0x1B):
  場景: 輸入"12"，按ESC，重新開始
  
  建議:
    ✅ ESC清除當前輸入緩衝區
    ✅ 顯示 "輸入已取消\r\n> "
```

```
Ctrl+C (0x03):
  問題: 是否需要處理？
  
  建議:
    - 嵌入式系統通常忽略
    - 或定義為清除緩衝區
```

#### 2.3 輸入頻率異常
**情況**: 輸入過快或異常快速輸入

```
場景1: 連續快速輸入
  操作: 連續按'5'十次（每次<100ms間隔）
  問題: 
    - 每次都回傳"輸入過快"嗎？
    - 會造成UART輸出洪水嗎？
  
  建議:
    ✅ 第一次顯示"輸入過快"後
    ✅ 設置標誌位，靜默拒絕後續輸入
    ✅ LED更新完成後清除標誌
    ✅ 避免重複輸出錯誤信息
```

```
場景2: 粘貼大量文字
  操作: 從別處複製粘貼大量文字到Tera Term
  問題:
    - 可能瞬間收到幾百個字符
    - UART緩衝區溢出？
  
  建議:
    ✅ UART驅動應該有硬體緩衝區（128 bytes）
    ✅ 軟體層限制處理速度
    ✅ 溢出時丟棄多餘字符
    ⚠️ 可能需要測試實際行為
```

---

### 3. 模式切換邏輯異常

#### 3.1 LED狀態轉換
**情況**: 切換模式時LED當前值如何處理？

```
場景1: 從模式A切換到模式B，當前LED=100
  初始: 模式A, LED=100 (01100100)
  切換: 改為模式B
  問題:
    - 100超出模式B範圍
    - LED應該保持還是清除？
  
  當前規格: "重置LED為全滅狀態"
  ✅ 正確決策
```

```
場景2: 模式內LED值在切換後仍有效
  初始: 模式B, LED=5, 當前輸入=3
  切換: 改為模式C
  問題:
    - 切換後LED=5在模式C中也有效
    - 但可能造成混淆（累進模式5應該是0-4都亮）
  
  當前規格: "重置LED為全滅狀態"
  ✅ 避免混淆，正確
```

#### 3.2 模式檢測時機
**情況**: 何時檢查DIP開關？

```
當前設計: 每100ms檢查一次
  
  問題:
    - 100ms內多次輸入不會檢測到模式變化
    - 可能造成短暫的模式不同步
  
  另一方案: 每次輸入前檢查
    優點: 即時響應
    缺點: 增加處理延遲
  
  ⚠️ 需要權衡:
    - 100ms對人類操作足夠快
    - 但自動化測試可能遇到問題
  
  建議:
    ✅ 保持100ms定時檢查
    ✅ 另外在每次輸入處理前也檢查一次
    ✅ 雙重保障
```

---

### 4. AXI總線與LED更新異常

#### 4.1 AXI寫入失敗
**情況**: AXI總線寫入超時或錯誤

```
可能原因:
  - PL端未正確初始化
  - 時鐘問題
  - 地址錯誤
  - AXI總線busy
  
  問題:
    - 如何檢測寫入失敗？
    - 系統如何反應？
  
  當前規格: "重試一次，若失敗顯示錯誤"
  
  ⚠️ 需要細化:
    - 重試間隔多久？
    - 錯誤信息是什麼？
    - LED狀態如何？
  
  建議:
    ✅ 實現帶超時的AXI寫入函數
    ```c
    int write_led_with_timeout(u8 value, u32 timeout_ms) {
        // 嘗試寫入
        Xil_Out32(LED_BASE_ADDR, value);
        
        // 等待並驗證（如果有狀態暫存器）
        u32 start = get_time_ms();
        while(get_time_ms() - start < timeout_ms) {
            if (read_led_status() == value) {
                return SUCCESS;
            }
        }
        return TIMEOUT;
    }
    ```
    ✅ 首次嘗試超時=10ms
    ✅ 重試一次，超時=50ms
    ✅ 失敗後顯示: "錯誤: LED更新失敗，請檢查硬體連接"
```

#### 4.2 LED更新狀態追蹤
**情況**: 如何知道LED更新完成？

```
問題:
  - PS端AXI寫入是即時的
  - 但物理LED可能需要時間
  - 如何確定"更新完成"？
  
  方案1: 固定延遲
    ✅ 寫入後等待5-10ms
    ✅ 簡單可靠
    ✅ 適用於本專案
  
  方案2: PL端提供狀態暫存器
    - 更複雜
    - 可能過度設計
  
  建議: 方案1
  
  實現:
    ```c
    volatile bool led_updating = false;
    
    void set_led(u8 value) {
        led_updating = true;
        Xil_Out32(LED_BASE_ADDR, value);
        usleep(10000);  // 10ms
        led_updating = false;
    }
    ```
```

#### 4.3 並發寫入問題
**情況**: 理論上可能同時有多個LED更新請求

```
場景: 
  - 用戶快速輸入兩次
  - 第一次LED還在更新
  - 第二次輸入到達
  
  當前設計: 檢查led_updating，拒絕第二次
  ✅ 正確
  
  但需要確認:
    - 檢查和設置led_updating之間有race condition嗎？
    - 在單核無RTOS環境下應該安全
    - 除非使用中斷
  
  ⚠️ 如果使用UART中斷接收:
    - 需要保護led_updating變量
    - 使用volatile關鍵字 ✅
    - 或禁用中斷保護關鍵區
```

---

### 5. UART通訊異常

#### 5.1 UART發送緩衝區滿
**情況**: 詳細反饋信息很長，可能填滿發送緩衝區

```
場景: 連續快速輸入有效值
  每次輸出: ~100字符（詳細模式）
  問題: UART TX緩衝區滿，會阻塞嗎？
  
  建議:
    ✅ 使用非阻塞或帶超時的發送
    ✅ 如果緩衝區滿，簡化輸出
    ✅ 或增加發送緩衝區大小
    
    檢查項:
    - XUartPs驅動的緩衝區大小
    - 是否需要調整
```

#### 5.2 UART接收溢出
**情況**: 輸入速度超過處理速度

```
場景: 粘貼大量文字
  問題: 
    - UART FIFO可能溢出（通常16-128 bytes）
    - 丟失字符
  
  檢測:
    ✅ UART狀態暫存器有溢出位
    ✅ 定期檢查
  
  處理:
    ✅ 檢測到溢出後清除緩衝區
    ✅ 顯示: "警告: UART接收溢出，請重新輸入"
```

#### 5.3 非ASCII字符
**情況**: 接收到亂碼或特殊字符

```
可能情況:
  - 鮑率設置錯誤
  - 通訊干擾
  - 用戶粘貼Unicode文字
  
  處理:
    ✅ 檢查字符範圍（0x20-0x7E可打印ASCII）
    ✅ 允許的控制字符: CR(0x0D), LF(0x0A), BS(0x08)
    ✅ 其他字符: 顯示"錯誤: 無效字符 (0xXX)"
```

---

### 6. 系統級異常

#### 6.1 上電初始化
**情況**: 系統剛上電時狀態不確定

```
可能問題:
  - DIP開關狀態抖動
  - UART還未穩定
  - PL端還未就緒
  - DDR初始化中
  
  建議:
    ✅ 上電後延遲500ms再開始
    ✅ 初始化順序:
        1. 系統時鐘
        2. DDR
        3. UART
        4. GPIO（DIP開關）
        5. AXI總線
        6. 讀取DIP，確定初始模式
        7. LED重置為全滅
        8. 顯示歡迎信息
    ✅ 每步檢查返回值
    ✅ 失敗則顯示錯誤並停止
```

#### 6.2 複位行為
**情況**: 按下複位按鈕

```
問題:
  - LED狀態會如何？
  - DIP開關可能已經改變
  
  建議:
    ✅ 複位後完全重新初始化
    ✅ 重新讀取DIP開關
    ✅ LED全滅
    ✅ 清除所有緩衝區
```

#### 6.3 看門狗超時
**情況**: 如果實現了看門狗

```
可能原因:
  - 程式進入死循環
  - AXI總線卡死
  - 中斷風暴
  
  建議:
    ✅ 主循環定期餵狗
    ✅ 超時後自動重啟
    ✅ 記錄錯誤原因（如有非易失性存儲）
```

---

### 7. 邊界值測試

#### 7.1 數值邊界
**測試用例表格**

| 模式 | 測試值 | 預期結果 | 需要驗證 |
|------|--------|----------|----------|
| A | 0 | ✅ LED=00000000 | ✅ |
| A | 1 | ✅ LED=00000001 | ✅ |
| A | 127 | ✅ LED=01111111 | ✅ |
| A | 128 | ✅ LED=10000000 | ✅ |
| A | 254 | ✅ LED=11111110 | ✅ |
| A | 255 | ✅ LED=11111111 | ⚠️ 最大值 |
| A | 256 | ❌ 錯誤:超出範圍 | ⚠️ 邊界+1 |
| B | 0 | ✅ LED=00000000 | ✅ |
| B | 8 | ✅ LED=LED7亮 | ⚠️ 最大值 |
| B | 9 | ❌ 錯誤:超出範圍 | ⚠️ 邊界+1 |
| C | 0 | ✅ LED=00000000 | ✅ |
| C | 8 | ✅ LED=11111111 | ⚠️ 最大值 |
| C | 9 | ❌ 錯誤:超出範圍 | ⚠️ 邊界+1 |

#### 7.2 緩衝區邊界
```
輸入緩衝區大小測試:
  最大長度: 3位數字 + '\0' = 4 bytes
  
  測試:
  - "255\0" ✅ 剛好4 bytes
  - "2555" ❌ 應該拒絕第4個'5'
  - "999" ✅ 接受，但範圍檢查時報錯
```

---

### 8. 狀態一致性問題

#### 8.1 軟體狀態與硬體狀態不同步
**情況**: LED實際狀態與程式記錄不一致

```
可能原因:
  - AXI寫入失敗但未檢測到
  - PL端邏輯錯誤
  - 外部手動改變LED（不可能，但理論上）
  
  問題:
    - STATUS暫存器讀回值與預期不符
  
  建議:
    ✅ 寫入後讀回驗證
    ```c
    void set_led_verified(u8 value) {
        Xil_Out32(LED_BASE_ADDR, value);
        usleep(1000);
        u8 readback = Xil_In32(STATUS_ADDR) & 0xFF;
        if (readback != value) {
            printf("警告: LED狀態不一致\r\n");
            printf("  預期: 0x%02X, 實際: 0x%02X\r\n", value, readback);
        }
    }
    ```
```

#### 8.2 模式記錄與DIP開關不同步
**情況**: 軟體記錄的模式與DIP不符

```
可能原因:
  - 讀取DIP開關失敗
  - 模式切換邏輯有bug
  
  建議:
    ✅ 不要記錄"當前模式"
    ✅ 每次需要時都讀取DIP開關
    ✅ 避免狀態不同步
    
    或者:
    ✅ 記錄當前模式，但定期重新讀取DIP驗證
```

---

### 9. 測試建議的異常場景

#### 9.1 壓力測試
```
測試1: 快速連續輸入
  操作: 1秒內輸入50次
  預期: 系統應該拒絕過快輸入，不崩潰
  
測試2: 長時間運行
  操作: 連續運行24小時，每10秒輸入一次
  預期: 無記憶體洩漏，無累積錯誤
  
測試3: 隨機輸入
  操作: 隨機字符，包含ASCII全範圍
  預期: 正確處理或拒絕所有輸入
```

#### 9.2 邊界測試
```
測試4: 所有邊界值（見7.1表格）
測試5: 所有無效輸入
測試6: 模式切換組合（所有6種切換路徑）
```

#### 9.3 異常恢復測試
```
測試7: 錯誤後恢復
  操作: 輸入無效值後，輸入有效值
  預期: 正常工作
  
測試8: 模式切換後功能
  操作: 每種模式都切換後測試
  預期: 每種模式功能正常
```

---

## ⚠️ 需要在規格中明確定義的項目

### 高優先級
1. ✅ **Backspace支持**: **已確認 - 不支持，顯示錯誤**
2. ✅ **模式A輸入緩衝區**: **已確認 - 4 bytes (存儲"255\0")**
3. ✅ **AXI寫入超時**: **已確認 - 10ms首次嘗試，50ms重試**
4. ✅ **DIP防抖時間**: **已確認 - 100ms**
5. ✅ **LED更新時間**: **已確認 - 使用PL端完成信號**
6. ✅ **快速輸入拒絕**: **已確認 - 第一次提示，後續靜默**

### 中優先級
7. ✅ **ESC鍵功能**: **已確認 - 支持，清除緩衝區**
8. ✅ **UART溢出檢測**: **已確認 - 檢測並報警**
9. ✅ **狀態回讀驗證**: **已確認 - 實現**
10. ⚠️ **錯誤計數**: 是否記錄累積錯誤？

### 低優先級
11. ✅ **看門狗**: **已確認 - 實現，超時後重啟並重新初始化**
12. 💡 **診斷模式**: 是否需要隱藏的測試命令？
13. 💡 **版本信息**: 如何查詢韌體版本？

---

## 📋 建議的補充測試用例

### 基本功能測試（已有）
- [x] 所有有效輸入值
- [x] 無效輸入值
- [x] 模式切換

### 異常情況測試（需要補充）
- [ ] 輸入過長數字（模式A）
- [ ] 輸入負數
- [ ] 模式切換期間輸入
- [ ] 快速連續輸入（壓力測試）
- [ ] DIP開關快速切換
- [ ] UART發送緩衝區滿測試
- [ ] AXI超時模擬（如果可能）
- [ ] 上電初始化多次測試
- [ ] 複位後恢復測試
- [ ] 所有邊界值測試

---

## 🔍 代碼審查檢查項

### PS端C代碼審查重點
```c
檢查項:
1. ✅ 所有數組訪問都有邊界檢查
2. ✅ 所有指針使用前檢查NULL
3. ✅ 整數溢出檢查（特別是atoi結果）
4. ✅ 除零檢查（如果有除法）
5. ✅ 資源洩漏檢查（malloc/free配對）
6. ✅ 死循環檢查（所有while需要退出條件）
7. ✅ 中斷安全（如使用中斷）
8. ✅ 變量初始化（特別是static和global）
9. ✅ 返回值檢查（所有函數調用）
10. ✅ 魔術數字定義為常量
```

### PL端VHDL代碼審查重點
```vhdl
檢查項:
1. ✅ 所有信號有複位值
2. ✅ 所有時序邏輯在同一時鐘域
3. ✅ 跨時鐘域有同步器
4. ✅ AXI握手邏輯正確
5. ✅ 沒有latches（所有if有else）
6. ✅ 數組訪問有邊界
7. ✅ 敏感度列表完整
8. ✅ 無組合迴路
```

---

## 📝 總結

### 發現的主要邏輯漏洞/不明確之處（已全部解決）

| # | 問題 | 嚴重性 | 決策 | 狀態 |
|---|------|--------|------|------|
| 1 | Backspace等編輯鍵未定義 | 中 | 不支持，顯示錯誤 | ✅ |
| 2 | 模式A緩衝區大小未定義 | 高 | 定義為4 bytes | ✅ |
| 3 | DIP開關抖動未處理 | 高 | 100ms防抖 | ✅ |
| 4 | AXI寫入無超時和重試機制 | 高 | 10ms首次，50ms重試 | ✅ |
| 5 | LED更新時間不確定 | 高 | **PL端提供完成信號** | ✅ |
| 6 | 快速輸入拒絕策略不清 | 低 | 第一次提示，後續靜默 | ✅ |
| 7 | 模式切換中途輸入未定義 | 中 | 清除緩衝區 | ✅ |
| 8 | UART溢出未處理 | 中 | 檢測並報警 | ✅ |
| 9 | 狀態驗證未實現 | 中 | 已實現回讀驗證 | ✅ |
| 10 | 上電初始化延遲未定義 | 低 | 500ms延遲 | ✅ |

---

### 重要決策摘要

✅ **DIP開關異常處理**:
- 輸入中切換: 清除緩衝區，重新輸入
- LED更新中切換: 等待完成→切換→LED全滅
- 快速切換: 100ms防抖，最小切換間隔100ms
- DIP=11無效: 禁用輸入，每0.5秒LED全部閃爍，每500ms重檢查
- 機械抖動: 100ms軟體防抖，兩次讀取一致才有效

✅ **輸入處理決策**:
- 緩衝區大小: 4 bytes
- 負數: 檢測'-'立即報錯
- 前導零: 允許
- 空輸入: 報錯並提示
- 非數字: Enter後檢查，報錯
- Backspace: 不支持，顯示錯誤
- ESC: 清除緩衝區
- Ctrl+C: 忽略

✅ **頻率控制決策**:
- 快速輸入: 第一次提示，後續靜默拒絕
- 粘貼文字: UART硬體緩衝128 bytes，軟體限速，溢出丟棄

✅ **LED更新機制** ⭐ (關鍵新需求):
- **PL端提供完成信號（STATUSbit 8）**
- **PS端輪詢完成標誌才允許下次輸入**
- **實現讀回驗證功能**
- AXI超時: 10ms首次，50ms重試
- 並發保護: 等待完成信號

✅ **UART決策**:
- 發送緩衝滿: 等待或簡化輸出
- 接收溢出: 檢測並報警
- 非ASCII: 檢查範圍，報錯

✅ **系統級決策**:
- 上電初始化: 500ms延遲，完整初始化流程
- 複位: 完全重新初始化
- 看門狗: 超時重啟，重新初始化

---

**結論**: 所有高優先級和中優先級問題已全部明確定義並補充到規格書中。

**關鍵改進**: 
1. ⭐ 新增STATUS暫存器（偏移0x04），bit 8為LED更新完成標誌
2. ⭐ PL端需要實現狀態機提供完成信號
3. ⭐ PS端需要實現輪詢檢查機制

**文檔版本**: 2.0（已確認所有決策）  
**審查日期**: 2026-03-16  
**下一步**: 規格已完整，可以開始VHDL和C代碼生成
