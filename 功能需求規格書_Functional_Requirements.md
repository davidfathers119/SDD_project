# 功能需求規格書 (Functional Requirements Specification)
## LED控制系統 - ZC702開發板

**文檔版本**: 1.0  
**日期**: 2026年3月16日  
**狀態**: 🔄 待確認  
**依賴文檔**: 硬體規格書 v1.0

---

## 1. 系統功能概述

### 1.1 主要功能
用戶通過PC端Tera Term終端輸入數字(0-8)，透過USB-UART傳輸至ZC702的ARM處理器，經由AXI總線傳送至FPGA，最終控制8個User LED的亮滅狀態。

### 1.2 功能流程圖
```
┌──────────┐    輸入     ┌──────────┐   USB-UART   ┌──────────┐
│  使用者  │─────0-8────>│Tera Term │─────────────>│   ZC702  │
│  (鍵盤)  │            │ (PC端)   │   115200bps  │  UART RX │
└──────────┘            └──────────┘              └────┬─────┘
                                                       │
                                                       ▼
  ┌────────────────────────────────────────────────────┐
  │              ZC702 內部處理流程                     │
  │                                                    │
  │  ┌──────┐  字符    ┌──────┐   AXI    ┌──────┐   │
  │  │ UART │─────────>│  ARM │─────────>│ FPGA │   │
  │  │  RX  │  接收    │  PS  │   Write  │  PL  │   │
  │  └──────┘          └──────┘          └───┬──┘   │
  │                       │                   │      │
  │                  驗證與處理            控制邏輯   │
  │                       │                   │      │
  └───────────────────────┼───────────────────┼──────┘
                          │                   │
                          ▼                   ▼
                    ┌──────────┐        ┌─────────┐
                    │  UART TX │        │ LED[7:0]│
                    │  (回顯)  │        │ 8個燈   │
                    └──────────┘        └─────────┘
```

---

## 2. 輸入需求規格

### 2.1 輸入來源
| 項目 | 規格 |
|------|------|
| **輸入設備** | PC鍵盤 |
| **輸入軟體** | Tera Term (或相容終端軟體) |
| **輸入方式** | 鍵盤數字鍵 (範圍根據模式變化) |
| **輸入頻率** | 必須等待LED更新完成才接受下一個輸入 |
| **頻率保護** | 輸入過快時回傳警告信息 |

### 2.2 有效輸入範圍（動態調整）

輸入範圍根據當前DIP開關選擇的模式動態變化：

| 模式 | DIP[1:0] | 有效輸入範圍 | ASCII碼範圍 | 說明 |
|------|----------|--------------|-------------|------|
| **模式A: 二進制顯示** | 00 | '0' - '255' | 需解析多位數字 | 可輸入0-255 |
| **模式B: 單燈模式** | 01 | '0' - '8' | 0x30 - 0x38 | 9個選項 |
| **模式C: 累進模式** | 10 | '0' - '8' | 0x30 - 0x38 | 9個選項 |
| **保留** | 11 | 無效 | - | 顯示錯誤 |

| 類型 | 範圍 | ASCII碼 | 說明 |
|------|------|---------|------|
| **有效輸入** | 根據模式 | 見上表 | 動態範圍 |
| **控制字符** | CR/LF | 0x0D / 0x0A | 回車換行（作為輸入確認） |
| **無效輸入** | 超出範圍 | - | 顯示錯誤和範圍提示 |

### 2.3 輸入處理需求

#### 2.3.1 有效輸入處理 (字符 '0' - '8')
```
輸入字符 → ASCII轉數值 → 設置LED → 回顯確認
   '0'   →      0       →  0x00   → "輸入值: 0\r\n..."
   '1'   →      1       →  0x01   → "輸入值: 1\r\n..."
   ...
   '8'   →      8       →  0x08   → "輸入值: 8\r\n..."
```

#### 2.3.2 無效輸入處理
```
輸入字符 → 驗證失敗 → 錯誤提示（含可輸入範圍） → 保持當前LED狀態
   'a'   →   無效   → "錯誤: 無效輸入 'a' (0x61)\r\n當前模式: 模式B (單燈模式)\r\n可輸入範圍: 0-8\r\n"
```

#### 2.3.3 控制字符處理
```
CR (\r) → 作為輸入確認（用於模式A多位數輸入）
LF (\n) → 忽略，不處理
```

#### 2.3.4 輸入頻率控制
```
輸入到達 → 檢查LED更新狀態 → 
  ├─ 已完成: 處理新輸入
  └─ 進行中: 回傳 "輸入過快，稍等片刻\r\n"
```

---

## 3. LED控制邏輯規格

### 3.1 模式選擇機制

**DIP開關控制** (來自Table 1-25: GPIO DIP Switch Connections)

| DIP[1:0] | 模式 | 說明 | 輸入範圍 |
|----------|------|------|----------|
| **00** | 模式A | 二進制顯示模式 | 0-255 |
| **01** | 模式B | 單燈模式 | 0-8 |
| **10** | 模式C | 累進模式 | 0-8 |
| **11** | 無效 | 保留 | - |

**DIP開關硬體定義**:
- DIP[0] = GPIO_DIP_SW0 (W6 pin, LVCMOS25)
- DIP[1] = GPIO_DIP_SW1 (W7 pin, LVCMOS25)

### 3.2 LED顯示模式詳細定義

#### 模式A: 二進制顯示模式 (DIP = 00)
**輸入範圍**: 0-255  
**輸入方式**: 多位數字，按Enter確認  
輸入數字直接以二進制形式顯示在LED上

| 輸入值 | 十六進制 | 二進制顯示 | LED7 | LED6 | LED5 | LED4 | LED3 | LED2 | LED1 | LED0 |
|--------|----------|-----------|------|------|------|------|------|------|------|------|
| 0 | 0x00 | 00000000 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ |
| 1 | 0x01 | 00000001 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 |
| 15 | 0x0F | 00001111 | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 | 🟢 | 🟢 | 🟢 |
| 85 | 0x55 | 01010101 | ⚫ | 🟢 | ⚫ | 🟢 | ⚫ | 🟢 | ⚫ | 🟢 |
| 170 | 0xAA | 10101010 | 🟢 | ⚫ | 🟢 | ⚫ | 🟢 | ⚫ | 🟢 | ⚫ |
| 255 | 0xFF | 11111111 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 |

**實作方式**: 
```c
// 模式A: 需要緩衝區累積輸入，按Enter確認
char buffer[4];  // 最多3位數 + \0
int value = atoi(buffer);
if (value >= 0 && value <= 255) {
    LED_CONTROL_REG = (uint8_t)value;
}
```

**輸入範例**:
```
> 123<Enter>     → LED = 01111011
> 255<Enter>     → LED = 11111111
> 0<Enter>       → LED = 00000000
```

#### 模式B: 單燈模式 (DIP = 01)
**輸入範圍**: 0-8  
**輸入方式**: 單一字符，立即生效
每次只點亮一個LED，位置對應輸入值

| 輸入值 | 點亮的LED | LED7 | LED6 | LED5 | LED4 | LED3 | LED2 | LED1 | LED0 |
|--------|-----------|------|------|------|------|------|------|------|------|
| 0 | 無 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ |
| 1 | LED0 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 |
| 2 | LED1 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 | ⚫ |
| 3 | LED2 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 | ⚫ | ⚫ |
| 4 | LED3 | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 | ⚫ | ⚫ | ⚫ |
| 5 | LED4 | ⚫ | ⚫ | ⚫ | 🟢 | ⚫ | ⚫ | ⚫ | ⚫ |
| 6 | LED5 | ⚫ | ⚫ | 🟢 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ |
| 7 | LED6 | ⚫ | 🟢 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ |
| 8 | LED7 | 🟢 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ |

**實作方式**:
```c
LED_CONTROL_REG = (input_value == 0) ? 0x00 : (1 << (input_value - 1));
```

#### 模式C: 累進模式 (DIP = 10)
**輸入範圍**: 0-8  
**輸入方式**: 單一字符，立即生效
從LED0開始累進點亮

| 輸入值 | 點亮範圍 | LED7 | LED6 | LED5 | LED4 | LED3 | LED2 | LED1 | LED0 |
|--------|----------|------|------|------|------|------|------|------|------|
| 0 | 無 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ |
| 1 | 0 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 |
| 2 | 0-1 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 | 🟢 |
| 3 | 0-2 | ⚫ | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 | 🟢 | 🟢 |
| 4 | 0-3 | ⚫ | ⚫ | ⚫ | ⚫ | 🟢 | 🟢 | 🟢 | 🟢 |
| 5 | 0-4 | ⚫ | ⚫ | ⚫ | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 |
| 6 | 0-5 | ⚫ | ⚫ | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 |
| 7 | 0-6 | ⚫ | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 |
| 8 | 0-7 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 |

**實作方式**:
```c
LED_CONTROL_REG = (1 << input_value) - 1;
```

### 3.3 模式切換行為

#### 3.3.1 模式切換檢測
- PS端定時讀取DIP開關狀態（每100ms讀一次）
- 檢渫到模式變化時立即回應

#### 3.3.2 模式切換提示信息
```
===============================================
模式切換: 模式A (二進制顯示模式)
===============================================

輸入範圍: 0-255
輸入方式: 輸入數字後按 Enter 確認
顯示邏輯: 以二進制形式顯示於8個LED
  例: 輸入23 → LED = 00010111

> _
```

#### 3.3.3 模式切換時的系統行為
1. 清除當前輸入緩衝區
2. 重置LED為全滅狀態
3. 顯示模式切換提示
4. 顯示新模式的輸入範圍和使用說明

---

## 4. 輸出需求規格

### 4.1 LED輸出
| 項目 | 規格 |
|------|------|
| **輸出數量** | 8個LED |
| **控制方式** | 並行控制（同時更新） |
| **更新時間** | < 1ms（從AXI寫入到LED更新） |
| **狀態保持** | 收到新命令前保持當前狀態 |

### 4.2 UART輸出（詳細反饋模式）
| 信息類型 | 格式 | 範例 |
|----------|------|------|
| **歡迎信息** | 系統啟動時顯示 | 見第6.1節 |
| **模式提示** | 模式切換時顯示 | 見第3.3.2節 |
| **確認信息** | 輸入成功後顯示 | 詳細：包含十進制、十六進制、二進制、LED列表 |
| **錯誤信息** | 無效輸入時顯示 | 包含當前模式和可輸入範圍 |
| **頻率警告** | 輸入過快時 | "輸入過快，稍等片刻\r\n" |

**詳細反饋範例** (模式B, 輸入3):
```
> 3
輸入值: 3 (0x03)
LED狀態: 00000011 (二進制)
點亮的LED: LED0, LED1

> 
```

---

## 5. 性能需求

### 5.1 響應時間需求
| 操作 | 要求 | 測量方法 |
|------|------|----------|
| **UART接收** | < 10ms | 從字符發送到接收完成 |
| **數據處理** | < 5ms | 從接收到AXI寫入 |
| **LED更新** | < 1ms | 從AXI寫入到LED變化 |
| **總響應時間** | < 100ms | 用戶可感知範圍 |

### 5.2 吞吐量需求
| 項目 | 要求 |
|------|------|
| **最大輸入頻率** | 10次/秒（實際由用戶輸入速度決定） |
| **UART緩衝** | 至少128 bytes |
| **處理延遲** | 不累積延遲 |

### 5.3 可靠性需求
| 項目 | 要求 |
|------|------|
| **連續運行時間** | > 24小時無故障 |
| **錯誤恢復** | 無效輸入不影響系統運行 |
| **狀態一致性** | LED狀態與暫存器值一致 |

---

## 6. 系統行為需求

### 6.1 初始化行為
```
上電/複位 → UART初始化 → GPIO初始化 → 讀取DIP開關 → 
  → 確定模式 → LED全滅 → 顯示歡迎信息 → 顯示模式提示 → 進入待命狀態
```

**初始化輸出範例** (假設DIP=00, 模式A):
```
===============================================
  LED Control System - ZC702
  版本: 1.0
===============================================

UART初始化成功 (115200 bps)
GPIO初始化成功
LED已初始化

===============================================
當前模式: 模式A (二進制顯示模式)
===============================================

輸入範圍: 0-255
輸入方式: 輸入數字後按 Enter 確認
顯示邏輯: 以二進制形式顯示於8個LED
  例: 輸入23 → LED = 00010111

> _
```

### 6.2 正常操作行為
```
待命 → 定時檢查DIP開關 → 
  ├─ 模式未變: 繼續等待輸入
  └─ 模式變化: 顯示模式切換提示 → 返回待命

接收字符 → 檢查LED更新狀態 → 
  ├─ 進行中: 顯示"輸入過快" → 返回待命
  └─ 完成: 驗證輸入 → 
      ├─ 有效: 更新LED → 顯示詳細確認 → 返回待命
      └─ 無效: 顯示錯誤（含模式和範圍） → 返回待命
```

### 6.3 錯誤處理行為
| 錯誤類型 | 系統行為 | 用戶反饋 |
|----------|----------|----------|
| **無效輸入** | 忽略，保持當前LED狀態 | 顯示錯誤、當前模式、可輸入範圍 |
| **超出範圍** | 忽略，不寫入PL | 顯示錯誤和當前模式的有效範圍 |
| **輸入過快** | 忽略，等待LED完成 | 顯示"輸入過快，稍等片刻" |
| **DIP無效** | LED全滅 | 顯示"無效模式，請調整DIP開關" |
| **UART錯誤** | 嘗試恢復通訊 | 無（透明處理） |
| **AXI超時** | 重試一次 | 若失敗顯示錯誤 |

**錯誤信息範例** (模式B, 輸入'9'):
```
> 9
錯誤: 無效輸入 '9' (0x39)
超出當前模式的輸入範圍

當前模式: 模式B (單燈模式)
可輸入範圍: 0-8
  0 = 全滅
  1-8 = 點亮LED0-LED7

> 
```

---

## 7. 異常處理詳細規格

### 7.1 DIP開關相關異常

#### 7.1.1 運行時模式切換 - 輸入進行中
**情況**: 用戶正在輸入數字（特別是模式A的多位數）時改變DIP開關  
**處理規則**:
1. 檢測到模式變化時，立即清除輸入緩衝區
2. 顯示模式切換提示信息
3. 請用戶重新輸入

**實現流程**:
```
每100ms檢查DIP → 模式變化檢測到 → 
  清除input_buffer[] → 
  重置buffer_index = 0 → 
  清除LED為全滅 →
  顯示模式切換提示 →
  等待新的輸入
```

#### 7.1.2 運行時模式切換 - LED更新進行中
**情況**: LED正在更新時用戶改變DIP開關  
**處理規則**:
1. 等待LED更新完成（led_updating = false）
2. 執行模式切換
3. 重置LED為全滅狀態
4. 顯示模式切換提示

**實現流程**:
```
模式變化檢測到 → 
  if (led_updating == true) {
    等待led_updating == false
  }
  切換模式 →
  LED_CONTROL_REG = 0x00 →  // 全滅
  顯示模式切換提示
```

#### 7.1.3 快速連續切換模式
**情況**: 用戶在短時間內（1秒內）多次切換DIP開關  
**處理規則**:
1. 實現100ms軟體防抖
2. 只響應穩定的模式變化
3. 最小切換間隔定義為10 0ms

**防抖實現**:
```c
uint8_t read_dip_debounced() {
    uint8_t first_read = read_dip_switches();
    usleep(100000);  // 等待100ms
    uint8_t second_read = read_dip_switches();
    
    if (first_read == second_read) {
        return second_read;  // 兩次讀取一致，認為有效
    } else {
        return 0xFF;  // 不一致，返回無效值
    }
}
```

#### 7.1.4 DIP開關讀取異常值 (DIP = 11)
**情況**: GPIO讀取到11（保留/無效模式）  
**處理規則**:
1. LED全部閃爍（每0.5秒全亮/全滅）作為警示
2. 禁用所有輸入處理
3. 每500ms重新檢查DIP開關
4. 檢測到有效模式時自動恢復

**實現流程**:
```c
while (1) {
    uint8_t mode = read_dip_switches();
    
    if (mode == 0x03) {  // DIP = 11 (無效)
        // 禁用輸入處理
        input_enabled = false;
        
        // LED閃爍警示
        LED_CONTROL_REG = 0xFF;  // 全亮
        usleep(500000);  // 500ms
        LED_CONTROL_REG = 0x00;  // 全滅
        usleep(500000);  // 500ms
        
        // 顯示錯誤信息（只顯示一次）
        if (!error_displayed) {
            printf("錯誤: 無效模式 (DIP = 11)\r\n");
            printf("請調整DIP開關到有效模式:\r\n");
            printf("  00 = 模式A (二進制顯示)\r\n");
            printf("  01 = 模式B (單燈模式)\r\n");
            printf("  10 = 模式C (累進模式)\r\n\r\n");
            error_displayed = true;
        }
    } else {
        // 恢復正常模式
        error_displayed = false;
        input_enabled = true;
        break;
    }
}
```

#### 7.1.5 DIP開關機械抖動
**情況**: 機械開關產生電氣抖動（如 00→01(5ms)→00(3ms)→01）  
**處理規則**:
1. 實現軟體防抖
2. 讀取到變化後等待100ms
3. 再次確認狀態
4. 兩次讀取一致才認為有效

**已在7.1.3實現**

---

### 7.2 輸入處理異常

#### 7.2.1 模式A - 輸入超大數字
**情況**: 用戶在模式A輸入超出範圍的數字（如999999）  
**處理規則**:
1. 限制輸入緩衝區為4個字符（存儲"255\0"）
2. 第4個字符後拒絕輸入（不回顯、不存儲）
3. 按Enter後進行範圍檢查
4. 超出範圍時顯示詳細錯誤信息

**實現**:
```c
#define INPUT_BUFFER_SIZE 4

char input_buffer[INPUT_BUFFER_SIZE];
int buffer_index = 0;

// 接收字符
if (isdigit(c)) {
    if (buffer_index < INPUT_BUFFER_SIZE - 1) {
        input_buffer[buffer_index++] = c;
        // 回顯
        printf("%c", c);
    } else {
        // 緩衝區滿，靜默拒絕
        // 不回顯、不存儲
    }
}

// Enter確認
if (c == '\r') {
    input_buffer[buffer_index] = '\0';
    int value = atoi(input_buffer);
    
    if (value > 255) {
        printf("\r\n錯誤: 輸入值%d超出範圍(0-255)\r\n", value);
        printf("當前模式: 模式A (二進制顯示模式)\r\n");
        printf("可輸入範圍: 0-255\r\n\r\n");
    }
    
    // 清除緩衝區
    buffer_index = 0;
}
```

#### 7.2.2 輸入負數
**情況**: 用戶輸入負號'-'  
**處理規則**:
1. 檢測到'-'字符立即報錯
2. 清除緩衝區
3. 顯示錯誤信息

**實現**:
```c
if (c == '-') {
    printf("\r\n錯誤: 不支持負數\r\n");
    printf("請輸入正整數\r\n\r\n");
    // 清除緩衝區
    buffer_index = 0;
}
```

#### 7.2.3 輸入前導零
**情況**: 用戶輸入如"007"  
**處理規則**:
1. 允許前導零
2. atoi()會自動解析為數值7
3. 正常處理

**無需特殊處理，atoi()會自動處理**

#### 7.2.4 只輸入Enter（空輸入）
**情況**: 用戶只按Enter鍵，沒有輸入任何數字  
**處理規則**:
1. 視為無效輸入
2. 顯示錯誤信息
3. 提示用戶輸入有效值

**實現**:
```c
if (c == '\r') {
    if (buffer_index == 0) {
        // 空輸入
        printf("\r\n錯誤: 未輸入任何值\r\n");
        printf("當前模式: %s\r\n", get_mode_name());
        printf("可輸入範圍: %s\r\n\r\n", get_valid_range());
        printf("> ");
        return;
    }
    // ... 正常處理
}
```

#### 7.2.5 輸入一半按Enter
**情況**: 模式A下輸入"2"後按Enter（單位數）  
**處理規則**:
1. 視為有效輸入
2. 2在範圍0-255內
3. 正常處理並更新LED

**無需特殊處理，按正常流程處理**

#### 7.2.6 輸入非數字字符
**情況**: 用戶輸入如"12a"  
**處理規則**:
1. 採用方案2：按Enter後檢查
2. 發現非純數字時報錯
3. 清除緩衝區

**實現**:
```c
if (c == '\r') {
    input_buffer[buffer_index] = '\0';
    
    // 檢查是否為純數字
    for (int i = 0; i < buffer_index; i++) {
        if (!isdigit(input_buffer[i])) {
            printf("\r\n錯誤: 包含非數字字符 '%c' (0x%02X)\r\n", 
                   input_buffer[i], input_buffer[i]);
            printf("請只輸入數字字符 (0-9)\r\n\r\n");
            buffer_index = 0;
            printf("> ");
            return;
        }
    }
    
    // 繼續處理有效輸入
    // ...
}
```

#### 7.2.7 特殊字符：Backspace (0x08)
**情況**: 用戶按Backspace鍵  
**處理規則**:
1. 不支持
2. 顯示錯誤信息

**實現**:
```c
if (c == 0x08) {  // Backspace
    printf("\r\n錯誤: 不支持Backspace編輯功能\r\n");
    printf("如需重新輸入，請按ESC清除當前輸入\r\n\r\n");
    printf("> ");
}
```

#### 7.2.8 特殊字符：ESC (0x1B)
**情況**: 用戶按ESC鍵  
**處理規則**:
1. 清除當前輸入緩衝區
2. 顯示取消信息
3. 重新顯示提示符

**實現**:
```c
if (c == 0x1B) {  // ESC
    buffer_index = 0;
    memset(input_buffer, 0, INPUT_BUFFER_SIZE);
    printf("\r\n輸入已取消\r\n> ");
}
```

#### 7.2.9 特殊字符：Ctrl+C (0x03)
**情況**: 用戶按Ctrl+C  
**處理規則**:
1. 嵌入式系統通常忽略
2. 不做任何處理

**實現**: 不處理此字符

---

### 7.3 輸入頻率異常

#### 7.3.1 連續快速輸入
**情況**: 用戶連續快速輸入（每次間隔<10ms）  
**處理規則**:
1. 第一次顯示"輸入過快"警告
2. 設置靜默標誌位
3. 後續輸入靜默拒絕（不顯示錯誤信息）
4. LED更新完成後清除標誌

**實現**:
```c
volatile bool led_updating = false;
bool fast_input_warning_shown = false;

// 在輸入處理函數中
if (led_updating) {
    if (!fast_input_warning_shown) {
        printf("輸入過快，稍等片刻\r\n");
        fast_input_warning_shown = true;
    }
    // 靜默拒絕後續輸入
    return;
}

// LED更新函數
void update_led(uint8_t value) {
    led_updating = true;
    Xil_Out32(LED_BASE_ADDR, value);
    
    // 等待PL端完成信號
    while (!(Xil_In32(STATUS_ADDR) & LED_UPDATE_COMPLETE_BIT)) {
        // 輪詢等待
    }
    
    led_updating = false;
    fast_input_warning_shown = false;  // 清除警告標誌
}
```

#### 7.3.2 粘貼大量文字
**情況**: 用戶從別處複製貼上大量文字  
**處理規則**:
1. UART驅動使用硬體緩衝區（128 bytes）
2. 軟體層限制處理速度
3. 溢出時丟棄多餘字符
4. 檢測UART溢出位並報警

**實現**:
```c
// 定期檢查UART狀態
uint32_t uart_status = XUartPs_ReadReg(UART_BASE_ADDR, XUARTPS_SR_OFFSET);

if (uart_status & XUARTPS_SR_OVER) {
    // 檢測到溢出
    printf("\r\n警告: UART接收溢出，請重新輸入\r\n");
    
    // 清除溢出標誌
    XUartPs_WriteReg(UART_BASE_ADDR, XUARTPS_SR_OFFSET, XUARTPS_SR_OVER);
    
    // 清除輸入緩衝區
    buffer_index = 0;
    
    printf("> ");
}
```

---

### 7.4 模式切換異常

#### 7.4.1 切換時LED狀態處理
**情況**: 任何模式切換  
**處理規則**:
1. 每當檢測到模式切換時
2. 立即將LED重置為全滅（0x00）
3. 避免狀態混淆

**實現**:
```c
uint8_t last_mode = 0xFF;  // 初始化為無效值

void check_mode_change() {
    uint8_t current_mode = read_dip_debounced();
    
    if (current_mode != last_mode && current_mode != 0xFF) {
        // 模式變化檢測到
        if (led_updating) {
            // 等待LED更新完成
            while (led_updating) {
                usleep(1000);
            }
        }
        
        // 重置LED為全滅
        LED_CONTROL_REG = 0x00;
        
        // 清除輸入緩衝區
        buffer_index = 0;
        
        // 顯示模式切換提示
        display_mode_change_message(current_mode);
        
        last_mode = current_mode;
    }
}
```

#### 7.4.2 模式檢測時機
**情況**: 確保不會漏檢模式變化  
**處理規則**:
1. 保持每100ms定時檢查DIP開關
2. 另外在每次輸入處理前也檢查一次
3. 雙重保障確保即時響應

**實現**:
```c
// 主循環定時檢查（每100ms）
void timer_callback() {
    check_mode_change();
}

// 輸入處理前也檢查
void process_input(char c) {
    check_mode_change();  // 立即檢查
    
    // 繼續處理輸入
    // ...
}
```

---

### 7.5 AXI總線與LED更新異常

#### 7.5.1 AXI寫入超時
**情況**: AXI總線寫入超時或錯誤  
**處理規則**:
1. 實現帶超時的AXI寫入函數
2. 首次嘗試超時=10ms
3. 失敗後重試一次，超時=50ms
4. 兩次都失敗則顯示錯誤信息

**實現**:
```c
typedef enum {
    AXI_SUCCESS = 0,
    AXI_TIMEOUT = 1,
    AXI_ERROR = 2
} axi_status_t;

axi_status_t write_led_with_timeout(uint8_t value, uint32_t timeout_ms) {
    // 寫入LED控制暫存器
    Xil_Out32(LED_BASE_ADDR, value);
    
    // 等待完成信號並驗證
    uint32_t start_time = get_time_ms();
    while (get_time_ms() - start_time < timeout_ms) {
        uint32_t status = Xil_In32(STATUS_ADDR);
        if (status & LED_UPDATE_COMPLETE_BIT) {
            // 驗證寫入值
            uint8_t readback = status & 0xFF;
            if (readback == value) {
                return AXI_SUCCESS;
            }
        }
        usleep(100);  // 短暫延遲
    }
    
    return AXI_TIMEOUT;
}

// 使用時
axi_status_t result = write_led_with_timeout(value, 10);  // 首次嘗試10ms
if (result != AXI_SUCCESS) {
    printf("警告: LED更新超時，正在重試...\r\n");
    result = write_led_with_timeout(value, 50);  // 重試50ms
    
    if (result != AXI_SUCCESS) {
        printf("錯誤: LED更新失敗，請檢查硬體連接\r\n");
        printf("  可能原因:\r\n");
        printf("  - PL端未正確初始化\r\n");
        printf("  - AXI總線連接問題\r\n");
        printf("  - 時鐘配置錯誤\r\n\r\n");
    }
}
```

#### 7.5.2 LED更新完成信號 ⭐ (重要新需求)
**情況**: PS端需要知道PL端LED更新何時完成  
**處理規則**:
1. PL端完成LED更新後，設置完成標誌位
2. PS端讀取狀態暫存器檢查完成標誌
3. 只有完成後才允許下一次輸入

**硬體需求** (需要在PL端實現):
```vhdl
-- 狀態暫存器定義 (偏移量 0x04)
-- Bit 8: LED更新完成標誌 (1=完成, 0=進行中)
-- Bit [7:0]: 當前LED狀態回讀

signal led_update_complete : std_logic;

-- LED更新邏輯
process(clk)
begin
    if rising_edge(clk) then
        if reset = '1' then
            led_update_complete <= '1';  -- 初始為完成狀態
        elsif axi_write_valid = '1' then
            -- 開始更新
            led_update_complete <= '0';
            led_output <= axi_write_data(7 downto 0);
            -- 可能需要幾個時鐘週期
        else
            -- 更新完成
            led_update_complete <= '1';
        end if;
    end if;
end process;

-- 狀態暫存器讀取
status_reg(8) <= led_update_complete;
status_reg(7 downto 0) <= led_output;
```

**軟體使用**:
```c
#define STATUS_ADDR         (LED_BASE_ADDR + 0x04)
#define LED_UPDATE_COMPLETE_BIT  (1 << 8)

bool is_led_update_complete() {
    uint32_t status = Xil_In32(STATUS_ADDR);
    return (status & LED_UPDATE_COMPLETE_BIT) != 0;
}

void update_led_with_wait(uint8_t value) {
    led_updating = true;
    
    // 寫入LED
    Xil_Out32(LED_BASE_ADDR, value);
    
    // 等待完成信號
    while (!is_led_update_complete()) {
        usleep(100);  // 短暫延遲
    }
    
    led_updating = false;
}
```

#### 7.5.3 並發寫入保護
**情況**: 防止同時有多個LED更新請求  
**處理規則**:
1. 使用led_updating標誌保護
2. 必須等待完成信號才能下次寫入
3. Volatile關鍵字確保可見性

**已在7.5.2實現**

---

### 7.6 UART通訊異常

#### 7.6.1 UART發送緩衝區滿
**情況**: 詳細反饋信息很長，可能填滿發送緩衝區  
**處理規則**:
1. 使用非阻塞或帶超時的發送
2. 如果緩衝區滿，簡化輸出
3. 或增加發送緩衝區大小

**實現**:
```c
// 檢查TX FIFO狀態
bool is_tx_fifo_full() {
    uint32_t status = XUartPs_ReadReg(UART_BASE_ADDR, XUARTPS_SR_OFFSET);
    return (status & XUARTPS_SR_TXFULL);
}

// 安全的printf包裝
void safe_printf(const char *format, ...) {
    if (is_tx_fifo_full()) {
        // 等待FIFO有空間，或超時
        uint32_t timeout = 100;  // 100ms超時
        while (is_tx_fifo_full() && timeout-- > 0) {
            usleep(1000);
        }
        
        if (timeout == 0) {
            // 超時，簡化輸出
            XUartPs_Send(&UartInstance, (u8*)"ERR\r\n", 5);
            return;
        }
    }
    
    // 正常輸出
    va_list args;
    va_start(args, format);
    vprintf(format, args);
    va_end(args);
}
```

#### 7.6.2 UART接收溢出
**已在7.3.2實現**

#### 7.6.3 非ASCII字符
**情況**: 接收到亂碼或特殊字符  
**處理規則**:
1. 檢查字符範圍（0x20-0x7E可打印ASCII）
2. 允許的控制字符: CR(0x0D), LF(0x0A), ESC(0x1B)
3. 其他字符顯示錯誤

**實現**:
```c
bool is_valid_character(char c) {
    // 可打印ASCII
    if (c >= 0x20 && c <= 0x7E) {
        return true;
    }
    
    // 允許的控制字符
    if (c == 0x0D || c == 0x0A || c == 0x1B) {
        return true;
    }
    
    return false;
}

void process_uart_char(char c) {
    if (!is_valid_character(c)) {
        printf("\r\n錯誤: 無效字符 (0x%02X)\r\n", (uint8_t)c);
        printf("只支持ASCII字符 (0x20-0x7E) 和部分控制字符\r\n\r\n");
        printf("> ");
        return;
    }
    
    // 繼續正常處理
    // ...
}
```

---

### 7.7 系統級異常

#### 7.7.1 上電初始化
**情況**: 系統剛上電時狀態不確定  
**處理規則**:
1. 上電後延遲500ms再開始
2. 按照規定順序初始化各模組
3. 每步檢查返回值
4. 失敗則顯示錯誤並停止

**初始化順序**:
```c
int system_init() {
    int status;
    
    // 1. 延遲500ms等待硬體穩定
    usleep(500000);
    
    // 2. 系統時鐘配置
    status = init_system_clock();
    if (status != XST_SUCCESS) {
        printf("錯誤: 系統時鐘初始化失敗\r\n");
        return status;
    }
    
    // 3. DDR初始化（由FSBL完成，這裡只驗證）
    // ...
    
    // 4. UART初始化
    status = init_uart();
    if (status != XST_SUCCESS) {
        // 無法通過UART報告，系統停止
        return status;
    }
    printf("UART初始化成功 (115200 bps)\r\n");
    
    // 5. GPIO初始化（DIP開關）
    status = init_gpio();
    if (status != XST_SUCCESS) {
        printf("錯誤: GPIO初始化失敗\r\n");
        return status;
    }
    printf("GPIO初始化成功\r\n");
    
    // 6. AXI總線驗證
    status = verify_axi_connection();
    if (status != XST_SUCCESS) {
        printf("錯誤: AXI總線連接失敗\r\n");
        return status;
    }
    
    // 7. 讀取DIP開關，確定初始模式
    uint8_t initial_mode = read_dip_debounced();
    printf("初始模式: ");
    display_mode_name(initial_mode);
    
    // 8. LED重置為全滅
    LED_CONTROL_REG = 0x00;
    printf("LED已初始化\r\n\r\n");
    
    // 9. 顯示歡迎信息
    display_welcome_message();
    
    // 10. 顯示當前模式使用說明
    display_mode_info(initial_mode);
    
    return XST_SUCCESS;
}
```

#### 7.7.2 複位行為
**情況**: 用戶按下複位按鈕  
**處理規則**:
1. 完全重新初始化
2. 重新讀取DIP開關
3. LED全滅
4. 清除所有緩衝區

**實現**: 複位後自動執行system_init()

#### 7.7.3 看門狗超時
**情況**: 如果實現看門狗，超時後的處理  
**處理規則**:
1. 看門狗超時觸發系統重啟
2. 重新執行完整初始化流程
3. 回到初始狀態

**實現**:
```c
// 主循環餵狗
while (1) {
    // 定期餵狗
    XWdtPs_RestartWdt(&WdtInstance);
    
    // 正常處理
    // ...
}

// 看門狗中斷處理（實際上不會執行，因為超時會直接重啟）
// 超時後硬體自動重啟，重新執行system_init()
```

---

### 7.8 異常處理優先級

| 優先級 | 異常類型 | 處理方式 | 是否阻塞系統 |
|--------|----------|----------|--------------|
| **P1 ​Critical** | DIP=11無效模式 | LED閃爍警示 | 是（禁用輸入） |
| **P1 Critical** | UART初始化失敗 | 無法運行 | 是（系統停止） |
| **P1 Critical** | AXI總線失敗 | 顯示錯誤 | 是（無法控制LED） |
| **P2 High** | AXI寫入超時 | 重試+錯誤提示 | 局部（單次操作失敗） |
| **P2 High** | UART接收溢出 | 清除緩衝+警告 | 否 |
| **P3 Medium** | 輸入過快 | 警告+拒絕 | 否 |
| **P3 Medium** | 無效輸入 | 錯誤提示 | 否 |
| **P4 Low** | 非ASCII字符 | 錯誤提示 | 否 |

---

## 8. 用戶介面需求

### 8.1 命令行介面 (CLI)
| 功能 | 需求 |
|------|------|
| **提示符** | ">" 表示等待輸入 |
| **回顯** | 顯示用戶輸入的字符 |
| **信息格式** | 清晰易讀，使用中文 |
| **換行** | 使用 \r\n (Windows風格) |

### 7.2 反饋信息格式（詳細模式）

**成功輸入** (模式A, 輸入23):
```
> 23
輸入值: 23 (0x17)
LED狀態: 00010111 (二進制)
點亮的LED: LED0, LED1, LED2, LED4

> 
```

**無效輸入** (模式B, 輸入'a'):
```
> a
錯誤: 無效輸入 'a' (0x61)
非數字字符

當前模式: 模式B (單燈模式)
可輸入範圍: 0-8
  0 = 全滅
  1-8 = 依序點亮LED0-LED7

> 
```

**輸入過快**:
```
> 3
輸入過快，稍等片刻
> 
```

---

## 9. 安全性需求

### 9.1 輸入驗證（在PS端實現）
⚠️ **關鍵要求**: 所有輸入驗證必須在PS端完成，只有有效輸入才能透過AXI傳輸至PL端

#### 9.1.1 驗證流程
```
接收輸入 → 讀取DIP開關 → 確定當前模式 → 
  ├─ 模式A: 檢查 0 ≤ value ≤ 255
  ├─ 模式B: 檢查 0 ≤ value ≤ 8
  └─ 模式C: 檢查 0 ≤ value ≤ 8
  
如果有效 → 寫入AXI暫存器 → 更新LED
如果無效 → 顯示錯誤（含範圍） → 不寫入PL
```

#### 9.1.2 驗證項目
- [x] 檢查輸入範圍（根據當前模式）
- [x] 過濾控制字符
- [x] 防止緩衝區溢位
- [x] 檢查LED更新狀態（頻率控制）
- [x] 驗證DIP開關狀態

### 9.2 系統保護
- [x] AXI寫入地址驗證
- [x] 無效輸入不影響系統穩定性
- [x] 防止意外的LED狀態變化
- [x] 模式切換時安全重置

---

## 10. 測試需求

### 10.1 功能測試用例
| 測試ID | 輸入 | 預期LED狀態 | 預期輸出 | 優先級 |
|--------|------|-------------|----------|--------|
| TC-01 | '0' | 根據模式定義 | 確認信息 | P1 |
| TC-02 | '1' | 根據模式定義 | 確認信息 | P1 |
| TC-03 | '2' | 根據模式定義 | 確認信息 | P1 |
| TC-04 | '3' | 根據模式定義 | 確認信息 | P1 |
| TC-05 | '4' | 根據模式定義 | 確認信息 | P1 |
| TC-06 | '5' | 根據模式定義 | 確認信息 | P1 |
| TC-07 | '6' | 根據模式定義 | 確認信息 | P1 |
| TC-08 | '7' | 根據模式定義 | 確認信息 | P1 |
| TC-09 | '8' | 根據模式定義 | 確認信息 | P1 |
| TC-10 | 'a' | 保持不變 | 錯誤信息 | P1 |
| TC-11 | '9' | 保持不變 | 錯誤信息 | P2 |
| TC-12 | '\r' | 保持不變 | 忽略 | P2 |
| TC-13 | 連續輸入 | 正確更新 | 每次確認 | P1 |

### 10.2 性能測試
- [ ] 響應時間測試（< 100ms）
- [ ] 連續輸入測試（至少100次）
- [ ] 長時間運行測試（24小時）

### 10.3 穩定性測試
- [ ] 快速連續輸入
- [ ] 隨機輸入組合
- [ ] 異常輸入處理

---

## 11. 驗收標準

### 11.1 必須滿足（Must Have）
- [x] 支持輸入0-8
- [x] LED正確顯示（根據選定模式）
- [x] UART通訊正常
- [x] 錯誤處理正確
- [x] 系統穩定運行

### 11.2 應該滿足（Should Have）
- [x] 響應時間 < 100ms
- [x] 用戶友好的反饋信息
- [x] 清晰的錯誤提示

### 11.3 可選滿足（Nice to Have）
- [ ] LED閃爍效果
- [ ] 更多顯示模式
- [ ] 配置保存功能

---

## 12. 依賴與約束

### 12.1 硬體依賴
- 依賴《硬體規格書》中定義的所有硬體資源
- 8個User LED必須可用
- UART J17必須連接

### 12.2 軟體約束
- 使用Xilinx提供的標準驅動
- C語言標準：C99或更高
- VHDL標準：VHDL-2008

---

## 13. 需要確認的項目

### 🔴 關鍵決策點（請確認）

1. **LED控制模式**（第3.1節）
   - [ ] 選項A: 二進制顯示
   - [ ] 選項B: 單燈模式
   - [ ] 選項C: 累進模式
   - [ ] 其他: _______________

2. **UART反饋信息細節程度**
   - [ ] 簡潔模式（只顯示結果）
   - [ ] 詳細模式（包含二進制、十六進制、點亮列表）

3. **錯誤處理方式**
   - [ ] 只顯示錯誤，不做其他動作
   - [ ] 顯示錯誤並提供幫助信息

---

## 14. 修訂歷史

| 版本 | 日期 | 修改者 | 說明 |
|------|------|--------|------|
| 1.0 | 2026-03-16 | - | 初始版本，待用戶確認LED模式 |

---

**文檔狀態**: ✅ 已完成（包含完整異常處理規格）  
**批准狀態**: 待用戶確認第13節關鍵決策點  
**下一步**: 確認後更新文檔並進入實作階段（生成VHDL和C代碼）
