# LED控制暫存器規格文檔

## 暫存器映射表

### 基地址
假設AXI外設基地址為: `0x43C00000`（需根據實際Vivado設計調整）

---

## 暫存器詳細定義

### 1. LED_CONTROL 暫存器 (偏移: 0x00)

**地址**: Base_Addr + 0x00  
**類型**: 讀/寫  
**複位值**: 0x00000000  

#### 位域定義

| 位 | 名稱 | 類型 | 複位值 | 描述 |
|----|------|------|--------|------|
| [31:8] | Reserved | RO | 0x000000 | 保留位，讀取為0 |
| [7] | LED7 | R/W | 0 | LED7控制位 (1=亮, 0=滅) |
| [6] | LED6 | R/W | 0 | LED6控制位 (1=亮, 0=滅) |
| [5] | LED5 | R/W | 0 | LED5控制位 (1=亮, 0=滅) |
| [4] | LED4 | R/W | 0 | LED4控制位 (1=亮, 0=滅) |
| [3] | LED3 | R/W | 0 | LED3控制位 (1=亮, 0=滅) |
| [2] | LED2 | R/W | 0 | LED2控制位 (1=亮, 0=滅) |
| [1] | LED1 | R/W | 0 | LED1控制位 (1=亮, 0=滅) |
| [0] | LED0 | R/W | 0 | LED0控制位 (1=亮, 0=滅) |

#### 使用範例（C代碼）
```c
#define LED_BASE_ADDR 0x43C00000
#define LED_CONTROL_REG (*(volatile uint32_t *)(LED_BASE_ADDR + 0x00))

// 點亮LED0
LED_CONTROL_REG = 0x01;

// 點亮LED0和LED1
LED_CONTROL_REG = 0x03;

// 熄滅所有LED
LED_CONTROL_REG = 0x00;

// 根據輸入值設置LED (0-8)
void set_led_by_value(uint8_t value) {
    if (value <= 8) {
        LED_CONTROL_REG = (uint32_t)value;
    }
}
```

---

### 2. STATUS 暫存器 (偏移: 0x04)

**地址**: Base_Addr + 0x04  
**類型**: 唯讀  
**複位值**: 0x00000000  

#### 位域定義

| 位 | 名稱 | 類型 | 複位值 | 描述 |
|----|------|------|--------|------|
| [31:8] | Reserved | RO | 0x000000 | 保留位 |
| [7:0] | LED_STATUS | RO | 0x00 | 當前LED狀態回讀 |

#### 使用範例（C代碼）
```c
#define STATUS_REG (*(volatile uint32_t *)(LED_BASE_ADDR + 0x04))

// 讀取當前LED狀態
uint8_t current_led = STATUS_REG & 0xFF;
```

---

## 輸入值與暫存器值對應表

| 輸入值 | 十六進制 | 二進制 | LED狀態 |
|--------|----------|--------|---------|
| 0 | 0x00 | 00000000 | 全滅 |
| 1 | 0x01 | 00000001 | LED0 |
| 2 | 0x02 | 00000010 | LED1 |
| 3 | 0x03 | 00000011 | LED0-1 |
| 4 | 0x04 | 00000100 | LED2 |
| 5 | 0x05 | 00000101 | LED0,2 |
| 6 | 0x06 | 00000110 | LED1-2 |
| 7 | 0x07 | 00000111 | LED0-2 |
| 8 | 0x08 | 00001000 | LED3 |

---

## 訪問時序

### 寫操作時序
1. ARM發起AXI寫事務
2. 提供地址（LED_CONTROL_REG偏移）
3. 提供寫數據（LED控制值）
4. 等待寫完成響應
5. LED輸出立即更新

### 讀操作時序
1. ARM發起AXI讀事務
2. 提供地址（STATUS_REG偏移）
3. 等待讀數據返回
4. 獲取當前LED狀態

---

## 注意事項

1. **寫入保護**: 寫入LED_CONTROL_REG的[31:8]位會被忽略
2. **原子性**: 每次寫入操作會同時更新所有8個LED
3. **讀回驗證**: 建議寫入後讀取STATUS寄存器驗證
4. **無效輸入**: 輸入值 > 8 應在軟體層面過濾，不應寫入硬體

---

**文檔版本**: 1.0  
**最後更新**: 2026-03-16
